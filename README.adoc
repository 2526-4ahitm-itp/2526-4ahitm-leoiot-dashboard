= LeoIoT Dashboard
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 2

IoT sensor monitoring dashboard for building management. Tracks temperature and CO2 levels across multiple rooms in real-time.

== Quick Start

[source,bash]
----
# Start all services
docker compose up

# Or start in background
docker compose up -d
----

== Access Points

[cols="^.^1,^.^3,^.<3", options="header"]
|===
| Service | URL | Description
| *3D Building View* | http://localhost:8080 | Interactive 3D building with room temperatures
| *Sensor Dashboard* | http://localhost:8081 | Clean dashboard with charts and sensor data
| *Grafana* | http://localhost:3000 | Advanced analytics and visualizations
| *InfluxDB* | http://localhost:8086 | Time-series database UI
|===


== Components

* **Mosquitto** - MQTT broker for sensor data
* **InfluxDB 2.7** - Time-series database
* **Telegraf** - MQTT to InfluxDB pipeline
* **Grafana** - Analytics dashboards
* **Quarkus Backend** - Sensor data generator
* **Frontend** - 3D building explorer (Three.js)
* **Dashboard-v2** - Modern sensor dashboard (Chart.js)
* **Fake Sensors** - Simulated sensor data for testing

== Development

[source,bash]
----
# View logs
docker compose logs -f dashboard-v2
docker compose logs -f fake-sensors

# Stop all services
docker compose down
----

== Configuration

[cols="^.^1,^.^1,^.<2", options="header"]
|===
| Service | Port | Environment
| Mosquitto | 1883 | -
| InfluxDB | 8086 | Auto-configured
| Grafana | 3000 | Admin / leogreen
| Frontend | 8080 | -
| Dashboard-v2 | 8081 | -
|===

---


== Deployment (School VM)
The project is deployed on `vm23.htl-leonding.ac.at`.

=== Remote Access (from home)
To access the services from outside the school network, use an SSH tunnel:
[source,bash]
----
ssh -L 8080:localhost:8080 -L 8081:localhost:8081 -L 3000:localhost:3000 -L 8086:localhost:8086 -L 8090:localhost:8090 solmqadmin@vm23.htl-leonding.ac.at
----


Once connected, you can reach the services at:

* **3D View:** http://localhost:8080
* **Dashboard:** http://localhost:8081
* **Grafana:** http://localhost:3000

=== Local Access (at school)
When connected to the school network (or VPN), access the services directly via the VM's hostname:

`http://vm23.htl-leonding.ac.at:<PORT>`


*Built with* MQTT, InfluxDB, Grafana, Quarkus, Three.js, Chart.js
