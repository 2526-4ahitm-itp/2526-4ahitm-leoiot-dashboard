name: Continuous Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy to Local Folder
        run: |
          cd /home/solmqadmin/2526-4ahitm-leoiot-dashboard
          git pull
          docker compose up -d --build
          
